import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Acceptable Use Policy', description: 'Rules for lawful and authorised use of AJN PDF tools.', alternates: { canonical: '/acceptable-use' } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
