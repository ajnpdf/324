import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Data Deletion Requests', description: 'How to delete AJN PDF browser preferences and request removal of directly provided information.', alternates: { canonical: '/data-deletion' } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
