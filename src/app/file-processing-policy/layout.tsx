import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'File Processing Policy', description: 'Understand browser and temporary server processing in AJN PDF.', alternates: { canonical: '/file-processing-policy' } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
