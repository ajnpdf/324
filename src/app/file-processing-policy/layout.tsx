import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'File Processing Policy', description: 'Understand AJN PDF file handling, on-device and online workflows, limits and cleanup behavior.', alternates: { canonical: '/file-processing-policy' } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
