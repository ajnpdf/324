$ErrorActionPreference="Stop"; $Z=Get-ChildItem "$env:USERPROFILE\Downloads" -Filter "AJN-PDF-3.1.0-CLOUD-RUN-PRODUCTION-R2*.zip" -File | Sort-Object LastWriteTime -Descending | Select-Object -First 1; if(-not $Z){throw "AJN PDF Cloud Run Production R2 ZIP was not found in Downloads."}; $R="$env:USERPROFILE\Desktop\AJN-PDF-GITHUB"; if(-not(Test-Path -LiteralPath $R)){throw "AJN-PDF-GITHUB was not found on Desktop."}; $T=Join-Path $env:TEMP ("AJN-PDF-CLOUDRUN-R2-"+[guid]::NewGuid().ToString("N")); New-Item -ItemType Directory -Path $T -Force | Out-Null; try { Expand-Archive -LiteralPath $Z.FullName -DestinationPath $T -Force; Copy-Item -Path (Join-Path $T "*") -Destination $R -Recurse -Force; Set-Location -LiteralPath $R; node .\scripts\verify-backend-workflow.mjs; if($LASTEXITCODE -ne 0){throw "Backend workflow verification failed."}; node .\scripts\verify-conversions.mjs; if($LASTEXITCODE -ne 0){throw "Conversion verification failed."}; node .\scripts\verify-code-quality.mjs; if($LASTEXITCODE -ne 0){throw "Code-quality verification failed."}; node .\scripts\verify-tool-ux.mjs; if($LASTEXITCODE -ne 0){throw "Tool UX verification failed."}; git add .; git status --short; git commit -m "AJN PDF Cloud Run backend production R2"; if($LASTEXITCODE -ne 0){ $S=(git status --porcelain); if($S){throw "Git commit failed."} else {Write-Host "No new commit was needed; working tree is already current." -ForegroundColor Yellow} }; git pull --rebase origin main; if($LASTEXITCODE -ne 0){throw "git pull --rebase failed."}; git push origin main; if($LASTEXITCODE -ne 0){throw "git push failed."}; Write-Host "AJN PDF CLOUD RUN R2 PUSH PASSED" -ForegroundColor Green; git log -1 --oneline } finally { Remove-Item -LiteralPath $T -Recurse -Force -ErrorAction SilentlyContinue }